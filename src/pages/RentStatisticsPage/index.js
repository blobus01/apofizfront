import React from 'react';
import RentStatisticsModule from "../../containers/RentStatisticsModule";

const RentStatisticsPage = (props) => {
  return (
    <RentStatisticsModule {...props} />
  );
};

export default RentStatisticsPage;