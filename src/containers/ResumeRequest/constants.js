export const SENDERS = Object.freeze({
  user: 'user',
  organization: 'organization',
})

export const VIEWS = Object.freeze({
  organization: 'organization',
  client: 'client'
})

export const RESUME_REQUEST_STATUSES = Object.freeze({
  in_progress: 'in_progress',
  accepted: 'accepted',
  rejected: 'rejected'
})